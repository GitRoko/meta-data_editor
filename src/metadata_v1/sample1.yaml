version: 1 # change version when file is changed
description: Consumer collected by the abracadabra.
group: Abracadabra
fields:
  id:
    js_type: number    
    optional: false
    db_type: integer
    # generator:
    #   type: zip
  name:
    js_type: string    
    optional: true
    db_type: varchar
    # generator:
    #   type: sample
    #   sample: name
